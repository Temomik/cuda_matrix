{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            // "command": "g++ *.cpp -O3 -o main.out -w && ./main.out",
            "command": "sh run.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}